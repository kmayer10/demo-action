name: Use Selenium Runner (example)

on:
  workflow_dispatch:

jobs:
  run-tests:
    runs-on: github-actions-scaleset-demo
    steps:
      - name: Run Selenium tests via shared action
        uses: ./
        with:
          APP_BASE_URL: "http://thinknyxapp-svc.staging.app"
          REPORT_PATH: "reports/selenium-report.html"
